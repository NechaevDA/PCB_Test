<UserControl x:Class="PCB_Test.UI.Controls.PreferencesTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PCB_Test.UI.Controls"
             xmlns:vm="clr-namespace:PCB_Test.UI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:PreferencesTabViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <local:ValidationTextBox Grid.Row="0" ValidationRules="{Binding OrderNameValidationRules}"/>
        <local:ValidationTextBox Grid.Row="1" ValidationRules="{Binding OrderLayersValidationRules}"/>
        <local:ValidationTextBox Grid.Row="2" ValidationRules="{Binding OrderDimensionsValidationRules}" InvalidMessage="Dimensions must be within 0.1 and 100 mm"/>
        <local:ValidationTextBox Grid.Row="3" ValidationRules="{Binding OrderQuantityValidationRules}" InvalidMessage="At least 1 PCB"/>
    </Grid>
</UserControl>
